apiVersion: v1
kind: Service
metadata:
  name: reflexio-backend
  namespace: reflexio
  labels:
    app.kubernetes.io/name: reflexio-backend
    app.kubernetes.io/part-of: reflexio
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: reflexio-backend
  ports:
    - name: http
      port: 8081
      targetPort: http
